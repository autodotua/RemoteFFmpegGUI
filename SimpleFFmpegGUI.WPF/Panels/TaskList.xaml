<UserControl
    x:Class="SimpleFFmpegGUI.WPF.Panels.TaskList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SimpleFFmpegGUI.WPF.Panels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:SimpleFFmpegGUI.Model;assembly=SimpleFFmpegGUI.Core"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <local:TaskDescriptionConverter x:Key="TaskDescriptionConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView ItemsSource="{Binding Tasks}">
            <ListView.ItemContainerStyle>
                <Style
                    BasedOn="{StaticResource DefaultListViewItemStyle}"
                    TargetType="{x:Type ListViewItem}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8" />
                    <Setter Property="Margin" Value="0,0,16,0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type model:TaskInfo}">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="4" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Type, Converter={StaticResource Enum2DescriptionConverter}}" />
                        <TextBlock
                            Grid.Column="2"
                            Text="{Binding ., Converter={StaticResource TaskDescriptionConverter}, ConverterParameter=IO}" />
                        <!--<TextBlock
                            Grid.Column="2"
                            Text="{Binding Inputs, Converter={StaticResource TaskDescriptionConverter}, ConverterParameter=Inputs}" />
                        <TextBlock
                            Grid.Column="4"
                            Text="{Binding Output, Converter={StaticResource TaskDescriptionConverter}, ConverterParameter=Output}" />-->
                        <TextBlock
                            Grid.Row="2"
                            Foreground="{Binding ., Converter={StaticResource TaskDescriptionConverter}, ConverterParameter=Color}"
                            Text="{Binding ., Converter={StaticResource TaskDescriptionConverter}, ConverterParameter=Status}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>